# Optional. Only needed for live analysis of custom requests typed on the home screen.
# The built-in Q4 launch demo scenario runs fully offline without this key.
OPENAI_API_KEY=
# Optional override, defaults to gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini
